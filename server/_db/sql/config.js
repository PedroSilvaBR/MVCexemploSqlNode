const config = { 
                '[apelido do banco]':{
                    user: '[usuário do banco]',
                    password: '[senha do banco]',
                    server: '[ip do servidor sql]', 
                    database: '[nome do banco]',
                    port: [porta do banco]}                    
                }

module.exports = config;